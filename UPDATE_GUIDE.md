# Hướng dẫn Cập nhật (Update Guide)

Vì đây là extension cài đặt thủ công (Unpacked), việc cập nhật sẽ thực hiện như sau:

## Khi nào có bản cập nhật?
Khi bạn mở Extension, nếu có phiên bản mới, một thông báo màu cam sẽ xuất hiện:
> 🚀 New version available! [Get it]

## Cách cập nhật

1. **Tải phiên bản mới**:
   - Click vào link **"Get it"** trên thông báo hoặc truy cập [Releases](https://github.com/laiduc1312209/pld-history-manager/releases).
   - Tải source code mới về.

2. **Cập nhật file**:
   - Giải nén và ghi đè (overwrite) toàn bộ file cũ trong thư mục extension của bạn bằng file mới.
   - *Lưu ý: Dữ liệu lịch sử và cài đặt của bạn KHÔNG bị mất (vì nó nằm trong browser storage).*

3. **Reload Extension**:
   - Mở trình duyệt, truy cập `chrome://extensions`.
   - Tìm **PLD History Manager**.
   - Click vào biểu tượng **Reload** (mũi tên vòng cung) 🔄.

Mọi tính năng mới sẽ được áp dụng ngay lập tức!
